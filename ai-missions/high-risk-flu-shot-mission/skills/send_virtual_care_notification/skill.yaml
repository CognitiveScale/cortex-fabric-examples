camel: 1.0.0
name: hw/virtual-care
title: Virtual Care Notification
description: Virtual care nurse calls members
inputs:
  - name: request
    title: request
    parameters:
      $ref: InterventionRequest
    routing:
      all:
        action: hw/virtual-care
        runtime: cortex/daemons
        output: response

outputs:
  - name: response
    title: response
    parameters:
      $ref: InterventionResponse

properties:
  - name: daemon.port
    title: Daemon Port
    description: port
    required: true
    type: Number
    defaultValue: 6000