camel: 1.0.0
name: hw/nursline
title: Nurseline Notification
description: Sends user notification to call Nurseline 
inputs:
  - name: request
    title: request
    parameters:
      $ref: InterventionRequest
    routing:
      all:
        action: hw/nursline
        runtime: cortex/functions
        output: response

outputs:
  - name: response
    title: response
    parameters:
      $ref: InterventionResponse

properties:
  - name: daemon.port
    title: Daemon Port
    description: port
    required: true
    type: Number
    defaultValue: 6000