camel: 1.0.0
name: m-content-job
title: m-content-job
description: Managed Content Job Skill

inputs:
  - name: request
    title: Job Request
    parameters:
      - name: params
        type: object
        description: Request Parameters
        required: true
    routing:
      all:
        action: m-content-job
        runtime: cortex/jobs
        output: result

outputs:
  - name: response
    title: Job Response
    parameters:
      - name: result
        type: object
        description: Job Response
        required: true

actions:
  - name: m-content-job
    type: job
    image: m-content-job:latest
    environmentVariables: '"TEST"="value"'
